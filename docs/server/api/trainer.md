# API: Тренажёр (урок и задания)

[Docs](../../README.md) → [Server](../README.md) → [API](README.md) → **Тренажёр**

Эндпоинты для получения следующего задания урока и отправки ответа. Логика: клиент запрашивает **одно** упражнение за раз; после успешной проверки ответа запрашивает следующее. Сервер сам выбирает, какое задание вернуть (см. [PRD, раздел 3.4.1](../../product/PRD.md)). Все роуты защищённые — требуется заголовок `Authorization: Bearer <accessToken>`.

---

## GET /v1/lessons/:lessonId/tasks/next

Возвращает **одно** задание для урока. Вызывается при открытии урока и после каждого успешно выполненного задания. Сервер выбирает задание по правилам: пока пользователь не прорешал все задания урока — только ещё не выполненные; когда все выполнены хотя бы раз — произвольный порядок с исключением последних 5 выполненных в этом уроке.

**Заголовки запроса**

| Заголовок        | Значение |
|------------------|----------|
| `Authorization`  | `Bearer <accessToken>` |

**Параметры пути**

| Параметр    | Тип   | Описание |
|-------------|--------|----------|
| `lessonId`  | string | Идентификатор урока. |

**Успешный ответ** — `200 OK`

Тело — один объект «задание»:

| Поле         | Тип   | Описание |
|--------------|--------|----------|
| `id`         | string | Идентификатор задания. |
| `type`       | string | Тип: `sentence_builder`, `multiple_choice` и т.д. |
| `question`   | string | Вопрос/подсказка (например, текст на русском для сбора предложения). |
| `options`    | array  | (для multiple_choice) Варианты ответа. |
| `words`      | array  | (для sentence_builder) Слова для выбора (включая лишние). |
| …            | …      | Остальные поля по типу задания (см. контент и [implementation](../implementation.md)). |

Если заданий в уроке нет или не осталось подходящих по правилам — на усмотрение контракта: `200` с пустым объектом / `null` или `404`. Рекомендуется `200` с полем `task: null` и полем `lessonProgress` (прогресс урока в %, флаг «все пройдены» и т.п.), чтобы клиент мог показать экран «Урок завершён».

**Ошибки**

| Код | Описание |
|-----|----------|
| 401 | Токен отсутствует или невалиден. |
| 403 | Нет доступа к уроку (например, урок в платной теме, подписка не активна). |
| 404 | Урок не найден. |
| 500 | Внутренняя ошибка сервера. |

---

## POST /v1/lessons/:lessonId/tasks/:taskId/answer

Отправка ответа пользователя на задание. Сервер проверяет ответ, сохраняет факт выполнения в БД (прогресс, антифарм, награды) и возвращает результат. После успешного ответа клиент запрашивает следующее задание через `GET /v1/lessons/:lessonId/tasks/next`.

**Заголовки запроса**

| Заголовок        | Значение |
|------------------|----------|
| `Authorization`  | `Bearer <accessToken>` |

**Параметры пути**

| Параметр    | Тип   | Описание |
|-------------|--------|----------|
| `lessonId`  | string | Идентификатор урока. |
| `taskId`    | string | Идентификатор задания. |

**Тело запроса**

Зависит от типа задания. Общий вариант:

| Поле    | Тип    | Обязательно | Описание |
|---------|--------|-------------|----------|
| `answer`| string \| array | да | Ответ: для `sentence_builder` — строка собранного предложения или массив слов в порядке; для `multiple_choice` — идентификатор выбранного варианта. |

Пример (сбор предложения):

```json
{
  "answer": "I always drink coffee in the morning"
}
```

Пример (выбор варианта):

```json
{
  "answer": "option-id-2"
}
```

**Успешный ответ** — `200 OK`

| Поле       | Тип    | Описание |
|------------|--------|----------|
| `correct`  | boolean| Верный ли ответ. |
| `correctAnswer` | string | (при ошибке) Правильный ответ для отображения. |
| `explanation`   | string | (опционально) Краткое пояснение. |
| `reward`   | object | (опционально) Начисление зернышек и т.д. |

Пример:

```json
{
  "correct": true,
  "reward": { "seeds": 2 }
}
```

**Ошибки**

| Код | Описание |
|-----|----------|
| 400 | Неверный формат тела (нет `answer`, неверный тип). |
| 401 | Токен отсутствует или невалиден. |
| 403 | Нет доступа к уроку. |
| 404 | Урок или задание не найдены. |
| 500 | Внутренняя ошибка сервера. |
